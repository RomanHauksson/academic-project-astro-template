---
interface Props {
  source: string;
}

const { source } = Astro.props;
---

<video class="rounded-lg" poster="" autoplay controls muted loop height="100%">
  <source src={source} type="video/mp4" />
</video>
